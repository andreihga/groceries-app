spring:
  datasource:
    url: "jdbc:h2:mem:testDb;INIT=CREATE SCHEMA IF NOT EXISTS groceries_app;DATABASE_TO_UPPER=false;NON_KEYWORDS=value,key"
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update